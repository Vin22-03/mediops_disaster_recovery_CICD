apiVersion: v1
kind: Service
metadata:
  name: mediops-blue-svc
  namespace: mediops
  labels:
    app: mediops-app
    color: blue
spec:
  type: NodePort
  selector:
    app: mediops-app
    color: blue
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
